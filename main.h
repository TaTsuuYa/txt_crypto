#ifndef MAIN_H
#define MAIN_H

/* libraries */
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

/* prototypes */
/* logic functions */
char *enc(char *str);
char *dec(char *str);

/* modulo */
int modulo(int dividend, int divisor);

#endif /* #ifndef MAIN_H */

